# Third-Party Notices — Rust dependencies

This section lists Datalith's Rust dependencies and the license under which each
is distributed. It is generated by `cargo-about` and must not be edited by hand.

## Overview

| License | Packages |
|---------|----------|
{{#each overview}}| [{{{name}}}](#{{id}}) | {{count}} |
{{/each}}
## License texts

{{#each licenses}}
### {{{name}}}

SPDX identifier: `{{id}}`

Used by:

{{#each used_by}}- `{{crate.name}}` {{crate.version}}{{#if crate.license}} — `{{crate.license}}`{{/if}}{{#if crate.repository}} — <{{crate.repository}}>{{/if}}
{{/each}}
```
{{{text}}}
```

{{/each}}
