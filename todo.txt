x decouple editor and renderer
    x Markdown
    x image
    x todo.txt
        x TxTodo.rs lib
    bases
        list
        table
        cards
        kanban
        calendar
    canvas
    graph

hot reload side bar and files preview/editor

custom themes editor (explore GPUI components)

right sidebar
    outline

format in edit (need to rebuild editor from scratch)
